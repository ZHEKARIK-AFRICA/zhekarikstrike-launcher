# Проверка адаптивной загрузки Google Drive — 4 сентября 2026

## Итог

Функциональность подтверждена. Адаптивность уменьшила полное время на этом наборе в среднем на **19,55%**. Это результат одного ABBA-сравнения на этом компьютере и соединении, не гарантия такого же выигрыша для всего клиента или другого пользователя.

| Прогон | Режим | До завершения записи файлов | Скачивание | Пик задач / HTTP |
|---|---|---:|---:|---:|
| A1 | Ровно 2 загрузки | 72,86 с | 68,01 с | 2 / 2 |
| B1 | Адаптивный | 53,64 с | 47,73 с | 3 / 3 |
| B2 | Адаптивный | 48,73 с | 45,62 с | 3 / 3 |
| A2 | Ровно 2 загрузки | 54,40 с | 51,18 с | 2 / 2 |

Выигрыш B1 против A1: **26,38%**. Выигрыш B2 против A2: **10,41%**. Средний выигрыш вычислен по отношению суммарных времён адаптивных и фиксированных прогонов. Оба условия утверждённого критерия выполнены.

Во всех четырёх прогонах подготовка файлов началась до окончания скачивания. После скачивания до завершения записи оставалось примерно 3–6 секунд. Максимальная наблюдённая интервальная сетевая скорость — около 13,9 МиБ/с; это не скорость всего соединения.

## Что именно проверено

- Настоящий общий загрузчик, Range-запросы, проверка compressed/raw/file SHA, адаптивная подготовка файлов, staging и транзакционная запись.
- Управляемый локальный HTTP-транспорт подтвердил настоящее появление третьей задачи и третьего запроса, сохранение увеличения при выигрыше, откат без выигрыша, запрет увеличения при очереди 256 МиБ, повторную пробу после уменьшения очереди, снижение при HTTP 429 и отсутствие фоновых задач/запросов после отмены.
- В реальных адаптивных прогонах контроллер сделал 2 → 3 → 2. Последнее снижение соответствует его существующему краткосрочному критерию оценки пробы; это не отменяет измеренного выигрыша всей операции. Пороги не менялись.
- Фиксированный режим удерживал ровно две задачи независимо от предложений адаптивного контроллера.

## Одинаковые условия и лимиты

359 целых файлов, 8 паков; на прогон **475 532 317 байт** загрузки и **1 081 232 425 байт** подготовленных файлов. Полный серверный manifest не изменялся. Выбор файлов, порядок паков и начальные реплики совпадали; каждый прогон получил пустой отдельный кэш.

Общий принятый объём паков — **1 902 129 268 байт (1,77 ГиБ)** при лимите 2 ГиБ. Он равен четырём плановым объёмам, дополнительного трафика повторных попыток не было. HTTP/TLS-заголовки и запросы метаданных в счётчик паков не входят.

Content SHA: `01a13dfb3448ce6c55ec2051d70ad61775cbe1c2fa322330542d3b879d9675db`.

Manifest SHA: `ac4ab8a152e3e0371c7a61f77f4e531d5ed163f7283ea5672c1fe47f182cc488`.

## Артефакты и безопасность

Исходный отчёт и полный manifest сохранены в `D:\zhekarik-adaptive-pack-probe\series-442710e6-dffe-4da2-971f-58e9008523da`. Внутри каждого `run.snapshot.decision_counts` находятся фактические счётчики решений. Поле `run.decisions` исходного отчёта — частоты причин в выборке снимков, включая дополнительные немедленные сводки, не число уникальных переключений.

После завершения задач все четыре помеченные папки `work-*` удалены. Проверочный `state.json` не оставлен как установленный клиент. Хэши состояния и конфигурации существующей E2E-установки до и после совпали. Launcher UI, UAC, игра и установщики компонентов не запускались. Backend, Drive и API не изменялись; новый EXE не публиковался.

После сравнения уточнены только диагностические записи: итоговая очередь обновляется перед финальным снимком; изменения числа распаковщиков пишутся сразу; фиксированный режим не сообщает о несуществующей смене числа загрузок. Исходные измерения сохранены без переписывания.

## Проверки

Форматирование прошло. Согласованный release-scope: **11 passed**, **1 ignored** (отдельный реальный Drive-тест). Реальный ignored-тест затем выполнен один раз: **1 passed**, четыре успешных прогона. Полный Rust-набор и очистка build cache не запускались.

После уточнения диагностических записей повторён только затронутый metrics-scope: **1 passed**. Финальная независимая проверка кода дополнительных замечаний не выявила. На D: осталось примерно **19,51 ГиБ** свободного места.

Правило субагентов сохранено глобально: `gpt-5.6-luna`, reasoning `low`; более сильная модель не выбирается без нового разрешения пользователя.
