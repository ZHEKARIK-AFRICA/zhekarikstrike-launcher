# Launcher 1.6.18: complete the approved Drive speed release

The user approved publishing every accumulated local optimization, including gains below 10%.

## Evidence and reason for the new version

- The complete local 1.6.17 gate on e61b468 passed: 57 frontend unit tests, 8 browser E2E, strict workspace fmt/clippy, 204 Rust tests (2 intentionally ignored network benchmarks), and 4 native Windows E2E. NSIS and portable artifacts were built; the portable SHA/size and streaming ED minisign were verified.
- PR #3 was merged normally as a5ea8cd; its tree matches e61b468. Tag v1.6.17 is preserved.
- GitHub run 33896261280 stopped before the Rust test suite/build/publication: Clippy 1.98 rejected two test-fixture chunks_exact_mut(8) loops. Local Rust/Clippy is 1.94. The live updater remained 1.6.13.
- 1.6.18 changes only version metadata and those two cfg(test) loops to as_chunks_mut::<8>().0. Both buffers have exactly 8 MiB, so generated bytes are identical. Runtime algorithms, thresholds, API and release gates are unchanged.

## Execution

- Fix both related lint findings together; verify fmt, strict workspace clippy, and the affected drive_pack_scheduler_tests scope locally.
- Do not repeat the already successful full local suite for this test-only compatibility fix. Do not add gate-skipping flags. GitHub runs the unchanged complete release.ps1 -Version 1.6.18 -Publish gate before signing/publication.
- Fetch fresh origin/tauri-rework, create/merge a normal PR without force-push, and push a new immutable v1.6.18 tag. Never retag v1.6.17.
- Verify CI success, the public portable SHA/size/streaming signature, updater discovery for 1.6.13/1.6.16, and the actual website download redirect.
- Preserve game content SHA 01a13dfb3448ce6c55ec2051d70ad61775cbe1c2fa322330542d3b879d9675db and all protected installation/configuration files.

The measured installation gain (4.25%) and repair gain (50.31%) remain subset measurements, not universal promises. No new benchmark series or game download is needed.
