# Launcher 1.6.17 Release Plan

> **For agentic workers:** Use executing-plans and verification-before-completion. User authorized production publication of all measured local improvements, including gains below 10%.

**Goal:** Publish signed portable 1.6.17 with all accumulated Drive-pack improvements and verify the public updater.

**Architecture:** Preserve launcher/backend protocol and Drive content. Integrate existing local commits into fresh origin/tauri-rework without force-push. Existing release pipeline provides local and GitHub gates, signing, immutable assets and admin manifest publication.

**Tech Stack:** Tauri/Rust workspace, PowerShell, GitHub Actions, minisign 0.12.

**Spec:** User production-release request; previous implementation and measured report at docs/superpowers/reports/2026-09-04-drive-pack-speed-next-local.md.

## Global Constraints

- Preserve all remote changes, existing tags/releases and user installation/config.
- Version1.6.17 supersedes public1.6.13 and locally distributed1.6.16; do not retag old versions.
- Tests batched in release gate; rerun only failed scopes while diagnosing. No cargo clean or unrelated cache deletion.
- New core included in fmt/clippy/test workspace checks. Backend and content pointers unchanged.
- Secrets remain outside Git/logs; validate release API credential with unchanged manifest and deliberately mismatched version0.0.0, require422 and unchanged active manifest.
- Agents only Luna/low. Root handles integration, signing and publication.

## Tasks

- [x] Inspect fresh origin, active updater and previous failed release; preserve isolated worktree and create codex/release-1.6.17. origin/tauri-rework is ancestor of local changes.
- [x] Set package/package-lock/Cargo/Cargo.lock/Tauri versions to1.6.17; expand release workspace gates and retain healthy cache; add release-script regression assertions.
- [x] Validate credential without activation: protected local copy was stale (401). Read the exact release credential from the active NY backend, require422 with unchanged manifest, synchronize GitHub secret without echoing it. Backend configuration unchanged.
- [ ] Commit/tag exact candidate locally. Run scripts/release.ps1 -Version1.6.17 once; diagnose/repeat failed scopes only. Verify signed portable size/SHA/minisign and native/browser E2E gate.
- [ ] Re-fetch origin, integrate ordinary merge/PR without force-push. Push only approved release commits/branch/tag; GitHub Actions performs final release gate with -Publish.
- [ ] Verify CI success, public portable/SHA/minisign, updater API and website download target; preserve prior version for rollback. Report evidence and any remaining user-interactive verification, without claiming unperformed E2E.

Installation gain4.25% and repair gain50.31% are subset measurements, not universal guarantees. Publication of both is now explicitly approved; no further benchmark series required.
